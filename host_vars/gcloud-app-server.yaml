app:
  env:
    server.port: "{{app_port}}"
    spring.datasource.username: appuser
    spring.datasource.password: apppassword
    spring.datasource.url: jdbc:postgresql://52.143.189.183:5432/appdb # db-vm ip

service:
  workingdir: "/home/zen-xaris/backend" # backend-vm username and working directory
  execstart: "java -jar target/springbootdemo-0.0.1-SNAPSHOT.jar"
